import React from "react";
// import createFastContext from "../utils/createFastContext";
// import { AdvancedSearchParams } from "../core/store/advancedSearchParams";

// const InitialPaginationParams = PaginationParams;
// const { Provider, useStore } = createFastContext(InitialPaginationParams);

// export const usePaginationParams = useStore;

// export function PaginationParamsContextProvider({ children }: { children: React.ReactNode }) {
//   return <Provider>{children}</Provider>;
// }
