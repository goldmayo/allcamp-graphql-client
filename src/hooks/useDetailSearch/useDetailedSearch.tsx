import { useContext } from "react";
// import { DetailedSearchParamsContext, DetailedSearchParamsDispatchContext } from "../../context/DetailedSearchContext";

// export function useDetailedSearchState() {
//   const state = useContext(DetailedSearchParamsContext);
//   if (!state) throw new Error("DetailedSearchParams Provider not found");
//   return state;
// }

// export function useDetailedSearchDispatch() {
//   const state = useContext(DetailedSearchParamsDispatchContext);
//   if (!state) throw new Error("DetailedSearchParams Provider not found");
//   return state;
// }
